<div class="row">
  <div class="col-md-7 col-centered">
    <div class="list-group">
      <a href="#" class="list-group-item active bg-color-title border-item">
        Users
      </a>
      <% @users.each do |user| %>
        <%= link_to user.email, user_audios_path(user.id), class: 'list-group-item bg-color-item border-item' %>
      <% end %>
    </div>
  </div>
</div>